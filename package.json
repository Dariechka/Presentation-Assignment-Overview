{
  "name": "presentation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "format": "prettier --write .",
    "preview": "vite preview",
    "deploy": "rm -rf dist && npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.5.3",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "reveal.js": "^5.2.1"
  }
}
